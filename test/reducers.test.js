import {
  user,
  authenticatedRequest,
  userActions
} from "../src/reducers"

const assert = require('chai').assert

describe("user reducer", () => {
  it("handles logins", () => {
    assert(false)
  })

  it("handles logouts", () => {
    assert(false)
  })
})

describe("authenticatedRequest reducer", () => {
  it("handles creation", () => {
    assert(false)
  })

  it("handles remove", () => {
    assert(false)
  })
})

describe("userActions reducer", () => {
  it("handles creation", () => {
    assert(false)
  })
})

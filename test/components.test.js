const assert = require("chai").assert

describe("AuthenticatedFallback", () => {
  it("should render without errors if not authenticated", () => {
    assert(false)
  })

  it("should not render if authenticated", () => {
    assert(false)
  })
})

describe("AuthenticatedView", () => {
  it("should render without errors if authenticated", () => {
    assert(false)
  })

  it("should not render if not authenticated", () => {
    assert(false)
  })
})

describe("AuthenticatedWrapper", () => {
  it("should render without errors", () => {
    assert(false)
  })

  it("should create user actions upon mounting", () => {
    assert(false)
  })
})

describe("LoginForm", () => {
  it("should render without error", () => {
    assert(false)
  })

  it("should render fallback if authenticated", () => {
    assert(false)
  })

  it("should submit login request upon form submission", () => {
    assert(false)
  })

  it("should dispatch login information upon success", () => {
    assert(false)
  })

  it("should dispatch authenticated request upon success", () => {
    assert(false)
  })

  it("should dispatch error", () => {
    assert(false)
  })
})

describe("Logout", () => {
  it("should render without error", () => {
    assert(false)
  })

  it("should dispatch logout upon click", () => {
    assert(false)
  })
})

describe("SignupForm", () => {
  it("should render without error", () => {
    assert(false)
  })

  it("should submit login request upon form submission", () => {
    assert(false)
  })

  it("should dispatch login information upon success", () => {
    assert(false)
  })

  it("should dispatch authenticated request upon success", () => {
    assert(false)
  })

  it("should dispatch error", () => {
    assert(false)
  })
})
